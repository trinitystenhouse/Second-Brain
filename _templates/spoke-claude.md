## What this spoke is for

<One paragraph: what this domain covers for the owner specifically, what they
already know, and what they are trying to get out of it. If there is a running
project or decision this spoke exists to serve, name it here — it becomes the
default anchor for anything practical.>

## Pipeline

`00_Inbox` (raw drops) → `01_Sources` (one note per real source) →
`02_Literature_Notes` (atomic concepts, optional — use when a single idea needs
its own note before it is ready for synthesis) → `03_Permanent_Notes` (the
owner's synthesis, in their own words) → `04_Maps_of_Content` (hubs with a
reading order).

## Domain rules

<Anything that applies here but not vault-wide. For example: which sources are
authoritative in this field and which are folklore; whether claims need
date-stamping; specialist vocabulary that should always be wikilinked; whether
this domain is sensitive and needs extra care.>

## Hub concepts

<Ten to twenty-five terms that recur in this domain. Wikilink these
aggressively wherever they appear — they are what makes the graph connect
rather than fragment.>

## Bridges

<Which other spokes this one genuinely connects to, and on what. Tag
cross-spoke notes `#bridge/<this-spoke>-<other-spoke>` so they surface in the
weekly collision run.>

## Conventions

Inherits everything in the root `claude.md`: kebab-case filenames, frontmatter
on every note, H2/H3 only, at least two links per note with stated reasons, no
fabricated sources, no empty stubs.
